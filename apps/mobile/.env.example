# Smart Parking Mobile — copy to apps/mobile/.env
# Required for auth, parking list, favorites, and analytics.

EXPO_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Optional — defaults shown in Settings only (no UI change when off)
# EXPO_PUBLIC_ENABLE_CITY_DATA_PREVIEW=false

# Optional — native map (EAS dev build only; ignored in Expo Go list MVP)
# EXPO_PUBLIC_ENABLE_MAP=false
# EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=
